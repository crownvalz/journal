journal-project/
│
├── app.py                       # Flask app for backend logic
├── requirements.txt             # Python dependencies for Heroku
├── Procfile                     # Heroku config to run the app
├── runtime.txt                  # Python version for Heroku (optional)
├── journal_entries.csv          # CSV file to store journal entries
├── customer_data.csv            # CSV file for customer data
├── gl_data.csv                  # CSV file for GL data
│
├── static/                      # Static files for Flask (e.g., images, styles)
│   ├── styles.css               # Your CSS file
│   └── script.js                # Your JavaScript file to interact with the backend
│
├── templates/                   # Flask templates for rendering dynamic HTML
│   └── index.html               # Your main HTML template (can be used for local development)
│
├── docs/                        # GitHub Pages directory (stores static files for deployment)
│   ├── index.html               # Static HTML for the journal page (GitHub Pages will serve this)
│   ├── styles.css               # Static CSS for GitHub Pages
│   └── script.js                # Static JS for GitHub Pages to call the backend
│
└── .gitignore                   # Git ignore file to prevent sensitive files from being pushed